{
  "name": "pm-command-center",
  "version": "1.0.0",
  "description": "Online PM dashboard with auto-reports, auto-assign, and live data from GitHub + Sheets + Discord",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "start": "vercel dev"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
